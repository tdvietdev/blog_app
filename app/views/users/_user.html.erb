<li>
  <%= gravatar_for user,"avatar_list", size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !user.current_user?(current_user) %>
    | <%= link_to t(".detele"), user, method: :delete, data: {confirm: t(".accept_del")} %>
  <% end %>
</li>
